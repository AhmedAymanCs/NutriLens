<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    
    <!-- الصلاحيات اللازمة للإنترنت والإشعارات (مهمة لنظام Android 13 فما فوق) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="nutrilens"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- تعريف القناة الافتراضية للإشعارات (تأكد أن الاسم يطابق الكود في Flutter) -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="Castly Notification" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />

            <!-- فلتر تشغيل التطبيق كـ Launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- فلتر النقر على الإشعارات (بدونه لن يتفاعل التطبيق عند الضغط على الإشعار) -->
            <intent-filter>
                <action android:name="FLUTTER_NOTIFICATION_CLICK" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>